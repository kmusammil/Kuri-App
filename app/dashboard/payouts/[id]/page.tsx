import Link from "next/link"; import { notFound, redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { preparePayout, markPayoutPaid } from "../actions";
export default async function PayoutDetailPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const{ id }=await params;const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const{data:rows,error}=await s.rpc("get_payout_for_admin",{target_winner_id:id});const payout=rows?.[0];if(error)redirect("/dashboard?error="+encodeURIComponent(error.message));if(!payout)notFound();return <main className="min-h-screen bg-slate-50 px-6 py-10 text-slate-900"><div className="mx-auto max-w-3xl"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-slate-500">Kuri-App</p><h1 className="mt-2 text-3xl font-bold">Payout</h1><p className="mt-2 text-sm">{payout.display_name||payout.registered_name}</p></div><Link href="/dashboard/payouts" className="rounded-lg border px-4 py-2.5">Back</Link></div><section className="mt-6 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">Gross</p><p className="mt-2 text-xl font-semibold">₹{Number(payout.gross_amount).toLocaleString("en-IN")}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">Muppu</p><p className="mt-2 text-xl font-semibold">₹{Number(payout.muppu_amount).toLocaleString("en-IN")}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">Net</p><p className="mt-2 text-xl font-semibold">₹{Number(payout.net_amount).toLocaleString("en-IN")}</p></div></section><section className="mt-6 rounded-2xl border bg-white p-6"><p>Status: <strong>{payout.status}</strong></p><p className="mt-2 text-sm text-slate-600">Reference: {payout.reference_number||"—"}</p>{payout.status!=="PAID"?<form action={markPayoutPaid} className="mt-5 space-y-4"><input type="hidden" name="winner_id" value={id}/><label className="block text-sm font-medium">Payment date<input className="mt-2 w-full rounded-lg border px-3 py-2.5" type="datetime-local" name="payment_date" required/></label><label className="block text-sm font-medium">Method<select className="mt-2 w-full rounded-lg border px-3 py-2.5" name="method"><option value="UPI">UPI</option><option value="BANK_TRANSFER">Bank transfer</option><option value="CASH">Cash</option><option value="OTHER">Other</option></select></label><label className="block text-sm font-medium">Other deductions (₹)<input className="mt-2 w-full rounded-lg border px-3 py-2.5" type="number" min="0" step="1" defaultValue="0" name="other_deductions"/></label><label className="block text-sm font-medium">Reference<input className="mt-2 w-full rounded-lg border px-3 py-2.5" name="reference"/></label><label className="block text-sm font-medium">Notes<textarea className="mt-2 w-full rounded-lg border px-3 py-2.5" rows={3} name="notes"/></label><button className="rounded-lg bg-slate-900 px-4 py-3 text-white" type="submit">Mark payout paid</button></form>:null}</section></div></main>}